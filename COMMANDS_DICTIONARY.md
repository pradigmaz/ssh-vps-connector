# Словарь команд SSH VPS Connector

## Что это?
Этот файл помогает понять какие команды можно разрешить в ALLOWED_COMMANDS.

## Как работает безопасность?
- Пустой ALLOWED_COMMANDS = ничего нельзя (только подключение)
- Указаны команды = можно только их

## 🟢 Безопасные команды (чтение)

### Просмотр файлов
- **ls** - показать список файлов
- **cat** - прочитать файл
- **head** - первые строки файла
- **tail** - последние строки файла
- **grep** - поиск в файлах
- **find** - найти файлы
- **locate** - быстрый поиск файлов
- **which** - найти путь к программе
- **file** - тип файла
- **stat** - подробная информация о файле
- **wc** - подсчёт строк/слов/символов
- **sort** - сортировка содержимого
- **uniq** - убрать дубликаты
- **diff** - сравнить файлы

### Информация о системе
- **ps** - список процессов
- **top** - мониторинг процессов
- **htop** - улучшенный мониторинг
- **df** - свободное место на дисках
- **du** - размер папок
- **free** - память
- **uptime** - время работы сервера
- **whoami** - текущий пользователь
- **id** - информация о пользователе
- **uname** - информация о системе
- **lscpu** - информация о процессоре
- **lsblk** - список дисков
- **mount** - подключённые диски
- **lsof** - открытые файлы
- **jobs** - фоновые задачи
- **history** - история команд

### Docker (чтение)
- **docker ps** - список контейнеров
- **docker logs** - логи контейнера
- **docker images** - список образов
- **docker inspect** - информация о контейнере
- **docker stats** - статистика контейнеров
- **docker version** - версия Docker
- **docker info** - информация о Docker

### Сеть (чтение)
- **ping** - проверка связи
- **curl** - HTTP запросы
- **wget** - скачать файл
- **netstat** - сетевые соединения
- **ss** - современная замена netstat
- **nslookup** - DNS запросы
- **dig** - расширенные DNS запросы
- **traceroute** - трассировка маршрута
- **iptables -L** - правила файрвола (только просмотр)

### Логи и мониторинг
- **journalctl** - системные логи
- **dmesg** - логи ядра
- **vmstat** - статистика виртуальной памяти
- **iostat** - статистика ввода-вывода
- **sar** - системная активность

## 🟡 Осторожно (изменения)

### Docker (управление)
- **docker start** - запустить контейнер
- **docker stop** - остановить контейнер
- **docker restart** - перезапустить контейнер
- **docker exec** - выполнить команду в контейнере
- **docker pull** - скачать образ
- **docker build** - собрать образ
- **docker-compose up** - запустить compose
- **docker-compose down** - остановить compose
- **docker-compose restart** - перезапустить compose

### Системные сервисы
- **systemctl status** - статус сервиса
- **systemctl start** - запустить сервис
- **systemctl stop** - остановить сервис
- **systemctl restart** - перезапустить сервис
- **systemctl reload** - перезагрузить конфигурацию
- **systemctl enable** - включить автозапуск
- **systemctl disable** - отключить автозапуск

### Файлы (изменение)
- **mkdir** - создать папку
- **touch** - создать файл
- **cp** - копировать
- **mv** - переместить
- **chmod** - права доступа
- **chown** - владелец файла
- **ln** - создать ссылку
- **rsync** - синхронизация файлов

### Архивы
- **tar** - работа с архивами
- **zip** - создать ZIP архив
- **unzip** - распаковать ZIP
- **gzip** - сжать файл
- **gunzip** - распаковать gzip

### Редактирование
- **nano** - простой редактор
- **vim** - продвинутый редактор
- **sed** - потоковое редактирование
- **awk** - обработка текста

### Процессы
- **kill** - завершить процесс
- **killall** - завершить процессы по имени
- **nohup** - запуск в фоне
- **screen** - виртуальные терминалы
- **tmux** - мультиплексор терминала

## 🔴 Опасные команды (НЕ рекомендуется)

### Удаление
- **rm** - удалить файл
- **rmdir** - удалить папку
- **rm -rf** - удалить всё рекурсивно
- **shred** - безопасное удаление
- **dd** - низкоуровневое копирование

### Форматирование
- **mkfs** - форматировать диск
- **fdisk** - разметка диска
- **parted** - управление разделами
- **cfdisk** - интерактивная разметка

### Перезагрузка
- **shutdown** - выключить сервер
- **reboot** - перезагрузить
- **halt** - остановить систему
- **poweroff** - выключить питание

### Системные изменения
- **passwd** - сменить пароль
- **useradd** - добавить пользователя
- **userdel** - удалить пользователя
- **usermod** - изменить пользователя
- **su** - сменить пользователя
- **sudo** - выполнить от имени root

## Примеры конфигурации

### Базовый (только чтение)
```json
"ALLOWED_COMMANDS": "ls,cat,grep,ps,docker ps,docker logs,df,free,uptime"
```

### Расширенный (чтение + Docker)
```json
"ALLOWED_COMMANDS": "ls,cat,grep,ps,docker ps,docker logs,docker start,docker stop,docker restart,systemctl status"
```

### Полный (всё кроме опасного)
```json
"ALLOWED_COMMANDS": "ls,cat,grep,ps,docker,systemctl,mkdir,cp,mv,chmod,nano,tar,kill"
```

### Администратор (почти всё)
```json
"ALLOWED_COMMANDS": "ls,cat,grep,ps,docker,systemctl,mkdir,cp,mv,rm,chmod,nano,tar,kill,useradd,passwd"
```

### Без ограничений (всё разрешено)
```json
"ALLOWED_COMMANDS": ""
```

## Как добавить команду?

1. Откройте конфигурационный файл вашего MCP клиента
2. Найдите секцию `ssh-vps-connector`
3. Добавьте в `env`:
```json
"ALLOWED_COMMANDS": "ls,cat,docker ps"
```
4. Перезапустите ваш MCP клиент

## Специальные случаи

### Команды с параметрами
Если разрешена команда `docker`, то разрешены все её подкоманды:
- `docker ps`
- `docker logs container_name`
- `docker exec -it container bash`

### Команды с опциями
Разрешение `ls` включает все опции:
- `ls -la`
- `ls -lh /var/log`
- `ls --color=auto`

### Пайпы и перенаправления
Работают автоматически если разрешены базовые команды:
- `ps aux | grep nginx`
- `cat file.txt | grep error`
- `ls > files.txt`

## Рекомендации по безопасности

### ✅ Рекомендуется разрешить
- Команды чтения: `ls`, `cat`, `grep`, `find`
- Мониторинг: `ps`, `top`, `df`, `free`
- Docker чтение: `docker ps`, `docker logs`
- Сеть: `ping`, `curl`, `wget`

### ⚠️ Разрешать осторожно
- Управление Docker: `docker start/stop/restart`
- Системные сервисы: `systemctl start/stop`
- Создание файлов: `mkdir`, `touch`, `cp`

### ❌ НЕ рекомендуется
- Удаление: `rm`, особенно `rm -rf`
- Системные изменения: `passwd`, `useradd`
- Перезагрузка: `shutdown`, `reboot`
- Форматирование: `mkfs`, `fdisk`

## Отладка

### Проверить разрешённые команды
Попробуйте выполнить простую команду: `ls`

### Если команда заблокирована
1. Проверьте `ALLOWED_COMMANDS` в конфигурации вашего MCP клиента
2. Добавьте нужную команду через запятую
3. Перезапустите ваш MCP клиент

### Логи безопасности
Все заблокированные команды записываются в логи MCP сервера.

## Автоматическая защита

### Запрещённые символы
Сервер автоматически блокирует команды с опасными символами:

**Заблокированные символы:**
- `;` - разделитель команд
- `&` - фоновое выполнение  
- `$` - подстановка переменных
- `` ` `` - выполнение команд в обратных кавычках
- `>` `<` - перенаправление (кроме безопасных случаев)

**Примеры блокировки:**
```bash
# ❌ Заблокировано
ls; rm -rf /tmp
cat /etc/passwd & 
echo $HOME
`whoami`
ls > /dev/null; rm file

# ✅ Разрешено
ls -la
docker ps
cat file.txt
```

### Заблокированные операции
Автоматически блокируются опасные операции:

**Privileged команды:**
- `sudo` - выполнение с правами root
- `su` - смена пользователя
- `passwd` - смена паролей

**Деструктивные подкоманды:**
- `rm -rf` - рекурсивное удаление
- `chmod 777` - открытие всех прав
- `dd if=/dev/zero` - затирание данных

**Path traversal:**
- `../../../etc/passwd` - выход за пределы папок
- `~/../.ssh/` - доступ к чужим файлам

**Примеры блокировки:**
```bash
# ❌ Заблокировано
sudo systemctl restart nginx
rm -rf /var/log
chmod 777 /etc/passwd
cat ../../../etc/shadow
dd if=/dev/zero of=/dev/sda

# ✅ Разрешено  
systemctl status nginx
rm file.txt
chmod 644 file.txt
cat /var/log/nginx/access.log
```

### Timeout защита
- Все команды имеют лимит **5 секунд**
- Предотвращает зависание системы
- Автоматическое завершение долгих процессов

**Примеры timeout:**
```bash
# ❌ Будет прервано через 5 секунд
find / -name "*.log" 2>/dev/null
sleep 10
tar -czf backup.tar.gz /var/www

# ✅ Выполнится быстро
ls /var/log
docker ps
ps aux | head -20
```

### Логирование попыток обхода
Все заблокированные команды записываются в логи:
- Время попытки
- Заблокированная команда  
- Причина блокировки
- IP адрес источника

**Пример лога:**
```
[2024-01-15 10:30:45] SECURITY: Blocked command "ls; rm -rf /" - dangerous separator
[2024-01-15 10:31:02] SECURITY: Blocked command "sudo systemctl restart" - privileged operation  
[2024-01-15 10:31:15] SECURITY: Command timeout "find / -name test" - exceeded 5s limit
```